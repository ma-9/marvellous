import { createUseStyles } from 'react-jss';

const useStyles = createUseStyles({
  title: {
    color: 'red',
    fontFamily: 'Muli',
    fontSize: '3rem',
    letterSpacing: 2
  },
  tagLine: {
    color: '#333',
    fontSize: '1rem',
    letterSpacing: 4
  }
});

export default useStyles;
